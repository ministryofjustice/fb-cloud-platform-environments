# auto-generated from templates in fb-cloud-platforms-environments
apiVersion: v1
kind: LimitRange
metadata:
  name: limitrange
  namespace: formbuilder-services-{{ .Values.environmentName }}
spec:
  limits:
  - default:
      cpu: 150m
      memory: 300Mi
    defaultRequest:
      cpu: 10m
      memory: 128Mi
    type: Container